var array = ["Patrick", "Rebecca", "Serri", "Marina"];
/*for (let index in array) {
   console.log(index);
 };

for (var value of array) {
   console.log(value);
};

*/
for (var index in array) {
    console.log("" + index + array[index]);
}
